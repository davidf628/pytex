%python
%a = nzrand(-4, 4)
%b = nzrand(-4, 4) where a != b
%c = randfrom([1, 3, 5, 7, 9])
%d = randfrom([1, 3, 5, 7, 9]) where c != d
%x = symbols('x')
%lhs = c / (x-a)
%rhs = d / (x-b)
%ineq = latex(lhs>=rhs)
%step1 = '$ \displaystyle ' + latex(c * (x-a) >= d * (x-b)) + '$'
%sol = sp.solve_univariate_inequality(lhs>=rhs, x, relational=False)
%end
Solve the inequality $ \displaystyle @ineq@$: \\[4mm]
\iskey{@step1@}
\iskey{$@latex(sol)@$}